# 私厨 AI（图片识别生成菜谱）设计报告

> 版本：v3.0　日期：2026-08-19　性质：练手项目（demo 级）
> 变更记录：
> - v1.1 将 AI 编排由 LangChain 固定函数流水线升级为 LangGraph 状态图（节点化 + 条件分支 + 可扩展）
> - v1.2 新增 6.5 节 Agent 代码参考（chief.py：DeepSeek 主脑 + Tavily 搜索 + LangSmith 追踪）
> - v1.3 chief.py 升级为「私人厨师」Agent：增加 GLM-4.6V 视觉识别，五步闭环（识图→清单→搜食谱→打分排名→建议报告）
> - v2.0 按新项目骨架整体重构：前端 uni-app → **Next.js**（编译产物由 FastAPI 静态托管）；接口从 recipe 单次生成改为 **chat 流式对话 + oss 签名上传**；后端结构重组为 `app/`（main / agents / api/v1 / models / common / static）；主脑统一为 **Agnes 多模态模型**（`agnes-2.5-flash`，直接读图，废除 GLM-4.6V 视觉代理）
> - v3.0 持久化方案升级为 **LangGraph + Checkpointer（方案 B）**：多轮记忆由 `langgraph-checkpoint` 的 `SqliteSaver` 接管（`thread_id` 会话维度），废除自建 `messages` 表双写；接口新增 `thread_id` 支持多会话；编排明确为 `create_agent(checkpointer=...)` + `astream_events` 流式

---

## 1. 项目概述

**一句话**：用户与「私人厨师」AI 对话，发送食材图片或文字，AI 直接看图识别食材，流式返回菜谱/建议，支持多会话历史回看。

**定位**：练手项目，UI 从简，功能只保留核心闭环，不做过度的产品化设计。

**技术栈**：

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Next.js | 编译为静态产物，由 FastAPI 直接托管（`app/static/`） |
| 后端 | FastAPI | Python 异步 Web 框架，流式对话走 SSE |
| AI 编排 | LangChain `create_agent` | 「私人厨师」Agent（返回 LangGraph `CompiledStateGraph`） |
| 主脑模型 | Agnes `agnes-2.5-flash` | OpenAI 兼容，多模态（`image_url` 直接读图），当前免费 |
| 搜索工具 | Tavily `tavily_search` | 以食材清单检索真实食谱（可选，缺 Key 则纯推理） |
| 持久化 | LangGraph Checkpointer（`SqliteSaver`） | 多轮记忆 + 会话状态快照，`thread_id` 区分会话 |
| 底层存储 | SQLite（`data/app.db`） | Checkpointer 的 checkpoint 落盘文件 |
| 图片存储 | OSS | 后端签发预签名上传 URL，前端直传对象存储 |

**已确认的决策**：
- 前端技术栈：Next.js（静态构建产物，FastAPI 托管，无需独立前端服务）
- 持久化：**LangGraph Checkpointer（方案 B）**——`create_agent(checkpointer=SqliteSaver)`，`thread_id` 即会话 id；多轮记忆、断点恢复由 Checkpointer 负责
- 主模型：Agnes `agnes-2.5-flash`（多模态直接读图，`AGNES_API_KEY` 配置于 `.env`）
- 图片上传：OSS 预签名 URL 直传（后端不落盘，`app/api/v1/oss.py` 签发）
- 交互形态：流式对话（`POST /chat/stream` 返回 SSE），历史可查可清，支持多会话（`thread_id`）
- 搜索工具 `tavily_search` 为可选项：未配置 `TAVILY_API_KEY` 时仅靠模型知识生成，不中断流程
- 历史查询/清空一律从 Checkpointer 派生（读最新 checkpoint 的 `messages`），**不做消息表双写**，保证数据一致

---

## 2. 系统架构

```
┌──────────────┐  对话(thread_id + 文字+图片URL)  ┌────────────────────┐
│   Next.js     │ ─────────────────────────────→ │       FastAPI        │
│   (app/static/│   POST /chat/stream            │  app/main.py         │
│    静态页面)   │ ←──────────────────────────── │  SSE 流式返回        │
└──────────────┘   流式回复(SSE)                 └────────┬───────────┘
                                                          │ agents/personal_chief.py
                                                          ▼
                                         ┌──────────────────────────────────┐
                                         │  Agnes 多模态主脑(直接读图识别食材) │
                                         │  ① 读图 → 食材清单                │
                                         │  ② tavily_search → 食谱候选       │
                                         │  ③ 打分排名 → 流式建议报告         │
                                         └──────────────┬───────────────────┘
                                                        │ create_agent(checkpointer=SqliteSaver)
                                                        │ config: {"configurable": {"thread_id": ...}}
                                                        ▼
                                         ┌──────────────────────────────────┐
                                         │  LangGraph Checkpointer           │
                                         │  (thread_id 会话维度状态快照)      │
                                         │  └─ 落盘 SQLite: data/app.db       │
                                         └──────────────────────────────────┘
   图片上传：前端 POST /oss/sign 取预签名 URL → 直传 OSS → 拿到图片 URL 再进对话
```

**数据流（一次完整对话）**：
1. 用户选图：前端请求 `POST /oss/sign` 拿预签名上传 URL，直传 OSS，得到图片 URL
2. 用户发送消息（`thread_id` + 文字 + 图片 URL）→ `POST /chat/stream`
3. 后端带 `configurable.thread_id` 调用「私人厨师」Agent（Agnes 多模态直接读图 → 识别食材 → Tavily 搜食谱 → 打分排名）
4. 模型输出以 **SSE 流式** 返回前端实时展示；每一步的中间状态由 **Checkpointer 自动落盘**（同一 `thread_id` 下次对话自动带上历史，实现多轮记忆）
5. 前端拉取历史：`GET /chat/messages?thread_id=...` 从该会话最新 checkpoint 读 `messages`；清空：删除该 `thread_id` 的全部 checkpoints

---

## 3. 功能需求（仅核心闭环）

| 编号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| F1 | 流式对话 | 文字 + 图片发送，AI 流式回复（SSE） | P0 |
| F2 | 图片直接识别 | 主脑多模态直接读图，识别食材并纳入生成流程 | P0 |
| F3 | 多轮记忆 | 同一 `thread_id` 连续对话，Agent 记得上文（Checkpointer 提供） | P0 |
| F4 | 多会话 | 不同 `thread_id` 相互隔离，各自有独立历史 | P1 |
| F5 | 历史记录 | 拉取指定会话历史消息列表，可回看 | P1 |
| F6 | 清空历史 | 一键清空指定会话 | P1 |
| F7 | 图片上传 | OSS 预签名 URL 直传，前端拿 URL 进对话 | P0 |
| F8 | 流式 loading | 生成过程有打字机/loading 反馈 | P0 |

**明确不做（YAGNI）**：登录注册、用户体系、会话列表页、收藏、点赞、评论、菜谱搜索页、多图识别、图片美化、分享、支付、推荐流。

---

## 4. 接口设计

统一约定：
- 请求/响应均为 JSON；流式接口为 `text/event-stream`（SSE）
- 响应包裹格式：`{"code": 0, "message": "ok", "data": {...}}`
- 会话维度统一用 **`thread_id`**（字符串，前端生成如 `UUID`）：新会话传新值，续聊传同一值

### 4.1 `POST /chat/stream`　流式对话

- **请求体**：

```json
{
  "thread_id": "a1b2c3d4-...",
  "message": "帮我看下这些食材能做什么菜",
  "image_url": "https://oss.xxx.com/xxx.jpg"
}
```

`thread_id` 必填（会话标识）；`image_url` 可选（多模态读图）；不传则纯文字对话。

- **响应**：`text/event-stream`，每帧为 JSON：

```json
{"delta": "你", "done": false}
{"delta": "好", "done": false}
...
{"delta": "", "done": true, "data": {"thread_id": "a1b2c3d4-..."}}
```

前端按 `delta` 追加展示；`done: true` 表示回复结束。

- **错误**：`thread_id` 为空或消息为空 → `{"code": 40001}`；Agent 调用失败 → `{"code": 50001, "message": "AI 服务异常，请稍后再试"}`

### 4.2 `GET /chat/messages`　获取会话历史

- **请求**：`?thread_id=a1b2c3d4-...&limit=50`
- **响应**：`data: {total, items: [{id, role, content, image_url, created_at}]}`，按时间正序（按对话顺序）
- 数据来源：从该 `thread_id` 最新 checkpoint 的 `state.messages` 派生，与对话内容一致

### 4.3 `DELETE /chat/messages`　清空会话历史

- **请求**：`?thread_id=a1b2c3d4-...`
- **实现**：删除该 `thread_id` 的全部 checkpoints（`SqliteSaver.delete_thread`）
- **响应**：`data: {deleted: true}`

### 4.4 `POST /oss/sign`　OSS 上传签名 URL

- **请求体**：`{"filename": "dish_xxx.jpg", "content_type": "image/jpeg", "size": 204800}`
- **响应**：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "upload_url": "https://oss.xxx.com/...?Signature=...",
    "public_url": "https://oss.xxx.com/xxx.jpg",
    "expires_in": 3600
  }
}
```

前端用 `upload_url` 直传（PUT），之后把 `public_url` 作为 `image_url` 发送对话。

---

## 5. 数据持久化（LangGraph Checkpointer）

方案 B：**不再自建 `messages` 表**，多轮记忆与会话状态由 `langgraph-checkpoint` 的 `SqliteSaver` 负责。

- 依赖：`langgraph-checkpoint`（当前 venv 已装 4.2.0）
- 存储文件：`data/app.db`（`SqliteSaver` 自动建表：`checkpoints` / `checkpoint_blobs` / `checkpoint_writes` 等，由框架维护）
- 会话维度：`thread_id`（`config["configurable"]["thread_id"]`）
- 初始化：

```python
import sqlite3
from langgraph.checkpoint.sqlite import SqliteSaver  # 或 AsyncSqliteSaver

conn = sqlite3.connect(DB_PATH, check_same_thread=False)
saver = SqliteSaver(conn)
```

- 挂载：`create_agent(model, tools, system_prompt, checkpointer=saver)`
- 历史派生：
  - 读：`await agent.aget_state(config)` → `state["messages"]`（按对话顺序）
  - 清：`await saver.adelete_thread(thread_id)`（或同步 `delete_thread`）
- 说明：demo 级用「读最新 checkpoint」即可，无需逐版本时间旅行；Checkpointer 的 `checkpoint_writes` 表在 sqlite 中串行写，量小无性能问题

> 与 v2.0 的取舍：v2.0 自建 `messages` 表需要手动双写（user/assistant），且无多会话恢复能力；Checkpointer 把「记忆 + 会话隔离 + 断点恢复」内建，代价是多一层抽象（thread_id / saver 生命周期）。demo 收益 > 成本。

---

## 6. AI 链路设计（LangChain Agent + Checkpointer）

### 6.1 主脑模型：Agnes 多模态（直接读图）

- 模型：`agnes-2.5-flash`（OpenAI 兼容，`https://apihub.agnes-ai.com/v1`），通过 `init_chat_model(model_provider="openai", base_url=AGNES_BASE_URL)` 初始化
- **多模态读图**：主脑直接支持 `image_url` 内容块，无需单独的视觉模型/视觉代理
- 图片要求：**可公网访问的图片 URL**（用户消息里的 `image_url`；本地文件需先走 OSS 上传）
- 配置：`.env` 中 `AGNES_API_KEY` / `AGNES_BASE_URL` / `AGNES_MODEL`
- 兜底：未配置 `AGNES_API_KEY` 时可回退 `DEEPSEEK_API_KEY`（纯文本，此时需另行处理图片）

### 6.2 搜索工具：Tavily（可选）

- `tavily_search`：以食材清单为关键词检索真实食谱
- 未配置 `TAVILY_API_KEY` 时仅靠模型知识生成食谱，不中断流程（降级容错）

### 6.3 编排方式：create_agent + Checkpointer

demo 级用 **Agent + 系统提示词驱动**（`create_agent` 返回 LangGraph `CompiledStateGraph`），并挂 **`SqliteSaver` Checkpointer**：

```python
agent = create_agent(
    model=model,
    tools=[search] if search else [],
    system_prompt=PRIVATE_CHEF_PROMPT,
    name="chief",
    checkpointer=SqliteSaver(sqlite3.connect(DB_PATH, check_same_thread=False)),
)
```

- 流式：`agent.astream_events({"messages": [HumanMessage(...)]}, version="v2", config={"configurable": {"thread_id": thread_id}})` 监听 `on_chat_model_stream` 产出增量
- 多轮记忆：同一 `thread_id` 再次调用时，Checkpointer 自动把历史 messages 拼进输入，Agent 记得上文（**无需手工拼历史**）
- 历史读取：`agent.aget_state(config)["messages"]`
- 清空会话：`saver.delete_thread(thread_id)`

> 说明：`create_agent` 本身就是 LangGraph 图，v2.0 的「自建 SQLite 表双写」可整体移除，收敛为 Checkpointer 单一数据源。

### 6.4 系统提示词主体（私人厨师）

```
你是一名专业、贴心的私人厨师。用户会提供一张食材图片（多模态输入，你可以直接查看图片），请严格按照以下流程工作：

1. 食材整理：直接读取用户消息中的图片，识别食材名称、大致用量、状态判断，
   整理成一份「当前可用的食材清单」。
2. 搜索食谱：以食材清单为检索关键词，调用 tavily_search 查找可行、真实的食谱；
   优先选择食材完全匹配的食谱，其次考虑可通过少量替换实现的。
3. 打分排名：从营养均衡度、制作难度两个维度对候选食谱分别打分（1-10 分），
   计算综合分并给出排名，简要说明打分理由。
4. 建议报告：将排序后的食谱整理成一份结构化建议报告，
   包含每道菜的评分与理由、所需补充的食材/调料、预估用时，帮助用户做出决策。

约束：以图片中实际可见的食材为准，不臆测；搜索不到合适食谱时如实说明。
```

### 6.5 Agent 参考实现（chief.py）

项目根 `agent-test/chief.py`（及同构副本 `c1.py`）为**可直接运行的 Agent 参考实现**（Agnes 多模态主脑 + Tavily 搜索 + LangSmith 追踪）。可作为 `personal_chief.py` 的模板；若在其上加 Checkpointer，只需 `create_agent(..., checkpointer=...)` 并在调用时传 `configurable.thread_id`。

### 6.6 关键风险与对策

| 风险 | 对策 |
|---|---|
| Agnes 免费模型限流/超时 | 后端捕获异常返回 `50001` 可读错误；`max_retries` 指数退避 |
| 图片不是食物/识别不出食材 | 主脑如实说明"未识别到食材，请换一张清晰的食材图片"，流程不崩溃 |
| 图片非公网 URL 不可读 | 前端强制走 OSS 上传拿公开 URL；后端校验 `image_url` 为 http(s) |
| 流式连接中断 | 前端 SSE 断线重连；Checkpointer 已落盘中间状态，重连可续 |
| 多会话串话 | `thread_id` 严格隔离；后端校验 `thread_id` 非空 |
| Checkpointer 锁/并发 | SqliteSaver 使用 `check_same_thread=False` + 进程内单例；量大再迁 PostgresSaver |
| Agent 节点内异常 | 单次对话 try/except 包裹，异常以 SSE `error` 帧返回并写入日志 |

---

## 7. 前端设计（Next.js）

### 7.1 页面结构

Next.js 项目编译产物输出到 `app/static/`（`index.html` + `_next/`），由 FastAPI 静态托管。

```
app/static/                 # Next.js 构建产物（fastapi StaticFiles 托管）
├── index.html              # 入口
└── _next/                  # 构建资源
```

- 单页即可（对话为主），无需多路由；如分页可用 `#/history` 锚点或路由懒加载
- 页面构成：会话选择（新对话/续聊）+ 消息列表 + 底部输入区（文本框 / 选图按钮 / 发送按钮）

### 7.2 页面说明（UI 极简）

- **会话**：进入页面生成一个 `thread_id`（`crypto.randomUUID()`），「新对话」按钮重置为新 id（本地状态，不落服务端会话表）
- **消息区**：滚动列表，user 气泡右对齐（图片以缩略图展示），assistant 气泡左对齐
- **输入区**：文字输入框 + 「📷 选图」按钮 + 「发送」按钮
- **流式展示**：fetch 读取 SSE，`delta` 增量追加到当前 assistant 气泡（打字机效果）
- **选图上传**：本地选图 → 压缩后请求 `POST /oss/sign` → PUT 直传 OSS → 得到 URL 填入待发消息
- **历史**：进入/切换会话时拉取 `GET /chat/messages?thread_id=...`；「清空」按钮调 `DELETE /chat/messages?thread_id=...`
- **loading**：等待首帧时显示「AI 思考中…」

### 7.3 请求封装

- `lib/api.ts`：封装 fetch，统一拼接 baseURL、解析 `{code, message, data}`、统一错误提示
- `lib/stream.ts`：封装 SSE 流式解析（`ReadableStream` + TextDecoder）
- baseURL 常量配置（本地 `http://localhost:8000`）

---

## 8. 后端项目结构

```
app/
├── main.py                    # FastAPI 入口：注册路由、CORS、静态目录
├── config.py                  # .env 加载、路径常量、SqliteSaver 连接初始化
├── agents/
│   └── personal_chief.py      # AI 代理核心逻辑（Agnes 多模态 + Tavily + Checkpointer）
├── api/
│   └── v1/
│       ├── chat.py            # 对话 API
│       │   ├── POST /chat/stream     流式对话（SSE，thread_id）
│       │   ├── GET  /chat/messages   获取会话历史（thread_id）
│       │   └── DELETE /chat/messages 清空会话（thread_id）
│       └── oss.py             # OSS 上传签名 URL（POST /oss/sign）
├── models/
│   └── schemas.py             # Pydantic 数据模型（请求/响应结构 + 统一 code/message/data 包装）
├── common/
│   └── logger.py              # 日志配置（统一 logger）
└── static/                    # Next.js 编译产出的静态网页（FastAPI 托管）
    ├── index.html
    ├── _next/
    └── ...                    # 其他静态资源
```

> 说明：不再有 `models/db.py`（自建 messages 表已废除），数据库连接收敛到 `config.py`（SqliteSaver 单例）。

依赖：`fastapi uvicorn[standard] python-multipart langchain langchain-openai langgraph langgraph-checkpoint langchain-tavily langsmith python-dotenv`（Pydantic 由 fastapi 自带）。

---

## 9. 开发计划

| 阶段 | 任务 | 产出/验收 |
|---|---|---|
| T0 | 环境准备 | venv + 依赖安装；Agnes API Key 填入 `.env` |
| T1 | 后端骨架 | 按 §8 目录建 `app/`；`main.py` 起服务、CORS、`/docs` 可访问；`common/logger.py` 生效 |
| T2 | Checkpointer | `SqliteSaver` 初始化（`data/app.db`）；`create_agent(checkpointer=...)` 构建成功 |
| T3 | AI 链路 | `agents/personal_chief.py` 跑通：Agnes 读图识别食材 + 流式输出 + 同 `thread_id` 多轮记忆（先用固定图片本地验证） |
| T4 | 对话 API | `/chat/stream`（SSE, thread_id）、`/chat/messages`（读 checkpoint / 删 thread）串通 |
| T5 | OSS 上传 | `/oss/sign` 签发预签名 URL，前端直传后图片 URL 可进对话 |
| T6 | 前端页面 | Next.js 对话页（流式展示 + 选图 + 会话/历史/清空）构建产物进 `app/static/` |
| T7 | 联调验收 | 完整走通「选图→上传→对话→流式看菜谱→多轮续聊→看历史/清空」；修 CORS 等联调问题 |

**验收标准（对照自查）**：
- [ ] 发送图片，AI 直接读图识别食材并流式返回菜谱建议
- [ ] 同一 `thread_id` 第二轮对话记得上文（无需重复描述）
- [ ] 不同 `thread_id` 会话隔离，历史互不串扰
- [ ] 历史可拉取、可清空，接口均返回约定 `{code, message, data}` 格式
- [ ] 未配置 Tavily Key 时纯推理不中断；识别不出食材给友好提示
- [ ] 代码结构清晰，注释到位，可直接运行

---

## 10. 环境与运行

### 后端

```bash
cd agent-test
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
# .env 里放 AGNES_API_KEY（必填）、TAVILY_API_KEY（可选）
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 前端

- Next.js 源码工程开发：`npm run dev` 独立调试
- 交付：`npm run build` 后把 `out/` 产物复制到 `app/static/`，由 FastAPI 托管（`GET /` → index.html）

### 联调注意

- 前端访问后端需 CORS（FastAPI `CORSMiddleware`，已包含在 T1）
- 本地图片必须走 OSS 拿公网 URL 才能被 Agnes 多模态读取
- `thread_id` 由前端生成并持久化（localStorage），刷新页面后凭同一 id 续聊

---

## 11. 后续可扩展（非本次范围）

- 会话列表页（枚举已有 `thread_id` + 标题）
- Agent 反思循环、识别失败自动重试、按厨具约束定制菜谱
- Checkpointer 升级 PostgresSaver / 云存储（多实例部署）
- `with_structured_output` 强类型输出
- 多图识别、菜谱随机换
- 登录 + 云端部署（云托管 / Docker）
